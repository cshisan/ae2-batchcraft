---
navigation:
  parent: index.md
  title: 快速开始
  position: 10
---

# 快速开始

先搭建最小可用网络，再配置频率绑定和产物返回。

1. 将输入端贴在样板供应器的输出面上。
2. 在处理机器旁放置一个或多个输出端。
3. 在输入端创建频率，并把它加载到所有端点。
4. 编码处理样板并发起合成任务。
5. 确认全部材料离开端点，且样板声明的产物成功返回。

> 默认的完整下发会保持一次处理推送不拆分。明确配置后，批次分发可以把处理样板划分成经过整数校验的份数。

<SubPages />
